# MXH Publisher v0.4.4

- Đóng gói `MXHPublisher.exe` theo Windows GUI subsystem.
- Khi nhấp đúp, mở từ shortcut hoặc chạy sau cài đặt, Windows không tạo cửa sổ CMD.
- Bổ sung smoke-test đọc trực tiếp PE header và chặn phát hành nếu EXE không phải GUI subsystem.
- Giữ nguyên hồ sơ Google Chrome dùng chung cho Facebook và TikTok.

Ứng dụng tiếp tục ghi chẩn đoán vào tệp log cục bộ thay vì cần cửa sổ console.
